# 服务器配置
server:
  host: 0.0.0.0
  port: 5000
  debug: false

# 测试配置
testing:
  timeout: 5
  concurrent: 10
  test_url: "https://www.google.com/generate_204"

# 订阅配置
subscriptions:
  update_interval: 3600
  auto_update: true
  max_nodes: 500

# 日志配置
logging:
  level: INFO
  file: logs/app.log
